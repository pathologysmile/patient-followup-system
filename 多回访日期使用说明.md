# 多回访日期功能说明

## 功能概述

现在系统支持为单个患者设置**多个回访日期**，方便进行长期随访管理。例如：出院后7天、30天、90天分别进行回访。

## 使用方法

### 1. 新患者登记时设置多个回访日期

在左侧边栏的"📝 新患者出院登记"表单中：

1. 填写患者基本信息（姓名、性别、住院号等）
2. 在"📅 设置回访日期"区域：
   - 系统默认显示第一个回访日期
   - 点击"➕ 添加回访日期"按钮，可以添加更多回访日期
   - 每个日期右侧有"❌"按钮，可以删除不需要的日期
   - 至少保留一个回访日期
3. 填写联系人信息
4. 点击"保存档案"

### 2. 界面示例

```
┌─────────────────────────────────────┐
│ 📝 新患者出院登记                    │
├─────────────────────────────────────┤
│ 住院号: [________]  姓名: [________]│
│ 性别: [男 ▼]       出院日期: [📅]   │
│                                     │
│ 出院诊断:                            │
│ [_________________________________] │
│ [_________________________________] │
│                                     │
│ 📅 设置回访日期                      │
│ 💡 提示：可以设置多个回访日期         │
│                                     │
│ 回访日期 1: [📅 2026-05-17]  [❌]   │
│ 回访日期 2: [📅 2026-06-10]  [❌]   │
│ 回访日期 3: [📅 2026-08-10]  [❌]   │
│                                     │
│ [➕ 添加回访日期]                     │
│                                     │
│ 联系人: [________]                   │
│ 联系电话: [________]                 │
│                                     │
│ [保存档案]                           │
└─────────────────────────────────────┘
```

## 技术实现

### 数据库结构

系统使用 `visit_plans` 表存储多个回访计划：

```sql
CREATE TABLE visit_plans (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    patient_id INTEGER NOT NULL,
    visit_date DATE NOT NULL,
    visit_type TEXT DEFAULT '常规回访',
    status TEXT DEFAULT 'pending',
    notes TEXT,
    completed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT (datetime('now', 'localtime')),
    FOREIGN KEY (patient_id) REFERENCES patients(id) ON DELETE CASCADE
)
```

### 代码逻辑

**前端（app.py）：**
- 使用 `st.session_state.visit_dates_list` 存储回访日期列表
- 动态添加/删除日期输入框
- 提交时将日期列表转换为字符串数组

**后端（db_manager.py）：**
- `add_patient()` 函数接收 `visit_dates` 参数（列表类型）
- 为每个回访日期创建一条 `visit_plans` 记录

```python
# 插入多个回访计划
for visit_date in visit_dates:
    cursor.execute('''
        INSERT INTO visit_plans (patient_id, visit_date, status)
        VALUES (?, ?, 'pending')
    ''', (patient_id, visit_date))
```

## 功能特点

### ✅ 优势

1. **灵活设置**：可以根据病情需要设置任意数量的回访日期
2. **动态管理**：随时添加或删除回访日期
3. **独立状态**：每个回访计划有独立的状态（pending/completed）
4. **智能提醒**：系统会在每个回访日期当天提醒医护人员

### 📊 应用场景

1. **癌症术后随访**：
   - 第1次：出院后7天（伤口检查）
   - 第2次：出院后30天（化疗评估）
   - 第3次：出院后90天（复查）
   - 第4次：出院后180天（长期随访）

2. **慢性病管理**：
   - 第1次：出院后3天（用药指导）
   - 第2次：出院后14天（病情评估）
   - 第3次：出院后30天（调整方案）

3. **手术后康复**：
   - 第1次：出院后5天（拆线）
   - 第2次：出院后15天（功能锻炼评估）
   - 第3次：出院后45天（康复进度）

## 查看和管理

### 智能提醒看板

在"📊 智能提醒看板"页面：
- **过期未回访**：显示所有已过期的回访计划
- **今日待回访**：显示今天需要回访的计划
- 每个回访计划独立显示，可以单独标记完成

### 数据查看页面

在"📋 患者数据查看"页面：
- 表格中显示患者的第一个回访日期
- 完整的多回访日期信息存储在数据库中

## 使用示例

### 示例1：胃癌术后患者

```
患者姓名：张三
出院日期：2026-05-10
回访日期设置：
  - 2026-05-17（出院后7天）
  - 2026-06-10（出院后30天）
  - 2026-08-10（出院后90天）
```

系统会在这三个日期分别提醒医护人员进行回访。

### 示例2：高血压患者

```
患者姓名：李四
出院日期：2026-05-10
回访日期设置：
  - 2026-05-13（出院后3天）
  - 2026-05-24（出院后14天）
  - 2026-06-10（出院后30天）
```

## 注意事项

1. **至少一个回访日期**：系统要求至少设置一个回访日期
2. **日期可以修改**：在智能提醒看板中可以修改单个回访计划的日期
3. **独立管理**：每个回访计划可以独立标记为完成
4. **数据兼容**：旧数据（单回访日期）仍然可以正常使用

## 相关文件

- [app.py](file:///D:/remindercode/app.py#L86-L160) - 前端表单实现（已更新）
- [db_manager.py](file:///D:/remindercode/db_manager.py#L44-L74) - 数据库操作（已支持多日期）
- [多回访日期功能说明.md](file:///D:/remindercode/多回访日期功能说明.md) - 本文档

## 常见问题

### Q1: 如何为患者添加新的回访日期？

A: 目前需要在登记时一次性设置。如需后续添加，可以在"数据查看"页面重置患者状态，然后重新编辑（未来版本会支持直接添加）。

### Q2: 如果某个回访日期错过了怎么办？

A: 在"智能提醒看板"的"过期未回访"区域，可以点击"📅 修改回访日期"按钮，为该计划设置新的日期。

### Q3: 如何查看患者的所有回访计划？

A: 可以通过数据库查询：
```sql
SELECT vp.visit_date, vp.status, vp.visit_type
FROM visit_plans vp
JOIN patients p ON vp.patient_id = p.id
WHERE p.name = '张三'
ORDER BY vp.visit_date ASC;
```

### Q4: CSV 导入支持多个回访日期吗？

A: 目前 CSV 导入只支持单个回访日期。如需导入多个日期，需要多次导入或使用数据库直接操作。
