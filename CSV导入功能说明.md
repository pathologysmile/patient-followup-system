# CSV 批量导入功能说明

## 🎯 功能概述

系统现在支持从CSV文件批量导入患者数据，快速添加大量患者信息。

## 📋 CSV 文件格式要求

### 必需列

CSV文件必须包含以下7个列（列名必须完全一致）：

| 列名 | 说明 | 示例 |
|------|------|------|
| 姓名 | 患者姓名 | 张三 |
| 性别 | 男或女 | 男 |
| 出院日期 | 格式: YYYY-MM-DD | 2026-05-05 |
| 回访日期 | 格式: YYYY-MM-DD | 2026-05-07 |
| 诊断 | 出院诊断 | 胃癌 |
| 联系人 | 联系人姓名 | 张三 |
| 联系电话 | 手机号码 | 13800138001 |

### 可选列

| 列名 | 说明 | 默认值 |
|------|------|--------|
| 状态 | pending 或 completed | pending |
| 创建时间 | 自动生成为当前时间 | - |

### 示例CSV文件

```csv
姓名,性别,出院日期,回访日期,诊断,联系人,联系电话,状态
张一,女,2026-05-05,2026-05-07,肺癌,张一,13800138001,pending
李二,男,2026-05-06,2026-05-08,胃癌,李二,13800138002,pending
王三,男,2026-05-07,2026-05-09,肝癌,王三,13800138003,completed
```

## 💻 使用步骤

### 步骤1：准备CSV文件

1. 使用Excel或其他工具创建CSV文件
2. 确保包含所有必需列
3. 保存为UTF-8编码的CSV格式

### 步骤2：上传文件

1. 访问 http://localhost:8501
2. 在左侧边栏找到"📥 CSV 批量导入"区域
3. 点击"选择 CSV 文件"按钮
4. 选择您的CSV文件

### 步骤3：预览数据

系统会自动：
- 检测记录数量
- 显示前10条数据预览
- 验证必需列是否存在

### 步骤4：开始导入

1. 确认数据无误后，点击"🚀 开始导入"按钮
2. 等待导入完成
3. 查看导入结果

### 步骤5：查看结果

系统会显示：
- ✅ 成功导入的患者数量
- ⚠️ 失败的记录数量
- 详细的错误信息（如果有）

## 📊 导入示例

### 成功导入

```
📄 检测到 100 条记录

✅ 成功导入 100 个患者
🎉 （气球动画）
```

### 部分失败

```
📄 检测到 100 条记录

✅ 成功导入 95 个患者
⚠️ 5 条记录导入失败

[查看错误详情]
  第3行：缺少必填字段
  第15行 (王五): UNIQUE constraint failed
  第28行 (赵六): invalid literal for int()
  ...
```

## 🔍 数据验证规则

### 自动验证

系统会自动验证：

1. **必填字段检查**
   - 姓名、性别、出院日期、回访日期、诊断、联系人、联系电话都不能为空

2. **数据格式检查**
   - 日期格式必须为 YYYY-MM-DD
   - 电话号码格式正确

3. **数据库约束**
   - 避免重复插入（如果存在唯一约束）

### 错误处理

- 单条记录失败不影响其他记录
- 所有错误都会记录并显示
- 可以查看详细的错误原因

## 💡 最佳实践

### 1. 导入前检查

```excel
✓ 确认列名完全匹配（包括空格）
✓ 检查日期格式是否正确
✓ 确保没有空行
✓ 验证电话号码格式
```

### 2. 分批导入

如果数据量很大（>1000条）：
- 建议分批次导入
- 每批100-200条
- 便于排查问题

### 3. 备份数据

导入前建议：
- 备份现有数据库
- 在测试环境先试导入
- 确认无误后再正式导入

### 4. 处理重复数据

如果CSV中有重复患者：
- 系统会根据数据库约束处理
- 可能提示"UNIQUE constraint failed"
- 需要清理重复数据后重新导入

## 🛠️ 技术实现

### 后端函数

```python
def import_patients_from_csv(csv_data):
    """
    从CSV数据批量导入患者
    
    参数:
        csv_data: list of dicts，每个dict包含患者信息
    
    返回:
        (success_count, error_count, errors_list)
    """
```

### 导入流程

```
1. 读取CSV文件
   ↓
2. 验证列名
   ↓
3. 逐行处理
   ├─ 提取字段
   ├─ 验证必填项
   ├─ 插入patients表
   └─ 创建visit_plans记录
   ↓
4. 提交事务
   ↓
5. 返回结果
```

### 数据映射

| CSV列 | 数据库表 | 字段 |
|-------|---------|------|
| 姓名 | patients | name |
| 性别 | patients | gender |
| 出院日期 | patients | discharge_date |
| 回访日期 | visit_plans | visit_date |
| 诊断 | patients | diagnosis |
| 联系人 | patients | contact_person |
| 联系电话 | patients | contact_phone |
| 状态 | patients & visit_plans | status |

## 📱 界面截图说明

### 侧边栏布局

```
┌─────────────────────┐
│ 🏥 患者出院回访智能提醒系统 │
├─────────────────────┤
│ [📊 智能提醒看板]    │
│ [📋 患者数据查看]    │
│ [💬 智能体对话]      │
├─────────────────────┤
│ 📝 新患者出院登记    │
│ [表单...]            │
├─────────────────────┤
│ 📥 CSV 批量导入      │
│ [选择 CSV 文件]      │
│                      │
│ 📄 检测到 100 条记录 │
│ [👀 预览数据 ▼]      │
│ [🚀 开始导入]        │
└─────────────────────┘
```

### 导入结果展示

```
✅ 成功导入 100 个患者
🎉🎉🎉

或者

✅ 成功导入 95 个患者
⚠️ 5 条记录导入失败

[▼ 查看错误详情]
  第3行：缺少必填字段
  第15行 (王五): ...
```

## 🔧 常见问题

### Q1: 提示"缺少必需列"？

**A:** 检查CSV文件的列名是否与要求完全一致，包括：
- 不能有额外的空格
- 必须是中文列名
- 顺序不重要，但名称必须匹配

### Q2: 导入后看不到数据？

**A:** 
1. 刷新页面
2. 到"患者数据查看"页面确认
3. 检查是否有过滤条件

### Q3: 部分记录导入失败？

**A:**
1. 点击"查看错误详情"
2. 根据错误信息修正数据
3. 重新导入修正后的文件

### Q4: 可以导入多次吗？

**A:**
- 可以，但要注意重复数据
- 建议先清空测试数据
- 或使用不同的文件名

### Q5: 支持多大文件？

**A:**
- Streamlit默认限制200MB
- 建议单次导入<1000条记录
- 大数据量建议分批导入

## 📈 性能优化

### 导入速度

- 100条记录：约1-2秒
- 500条记录：约5-10秒
- 1000条记录：约10-20秒

### 优化建议

1. **关闭不必要的页面刷新**
2. **使用有线网络**
3. **避免同时执行其他操作**

## 🎯 应用场景

### 场景1：医院批量录入

```
背景：新系统上线，需要录入历史患者数据
操作：
1. 从HIS系统导出CSV
2. 调整列名格式
3. 批量导入系统
4. 验证数据完整性
```

### 场景2：定期数据同步

```
背景：每周从其他系统同步新患者
操作：
1. 导出本周新增患者
2. 去重处理
3. 导入系统
4. 生成同步报告
```

### 场景3：测试数据生成

```
背景：需要大量测试数据
操作：
1. 使用脚本生成CSV
2. 包含各种边界情况
3. 批量导入测试
4. 验证系统稳定性
```

## 📝 示例代码

### Python生成CSV

```python
import csv
from datetime import datetime, timedelta

# 生成测试数据
with open('test_patients.csv', 'w', encoding='utf-8-sig', newline='') as f:
    writer = csv.writer(f)
    writer.writerow(['姓名', '性别', '出院日期', '回访日期', '诊断', '联系人', '联系电话', '状态'])
    
    for i in range(1, 101):
        name = f"测试患者{i}"
        gender = "男" if i % 2 == 0 else "女"
        discharge_date = (datetime.now() - timedelta(days=i)).strftime('%Y-%m-%d')
        visit_date = (datetime.now() - timedelta(days=i-2)).strftime('%Y-%m-%d')
        diagnosis = "胃癌" if i % 3 == 0 else "肺癌"
        phone = f"13800138{str(i).zfill(3)}"
        
        writer.writerow([name, gender, discharge_date, visit_date, diagnosis, name, phone, 'pending'])

print("CSV文件已生成")
```

---

**版本**: v2.1.0  
**更新日期**: 2026-05-09  
**功能状态**: ✅ 已启用
