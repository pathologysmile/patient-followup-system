# 错误边界处理功能说明

## 📋 功能概述

系统已实现**完善的错误边界处理机制**，在数据加载失败时提供友好的错误提示和恢复选项，提升系统的健壮性和用户体验。

## ✨ 核心特性

### 1. 全面的错误捕获

```python
try:
    # 数据加载逻辑
    patients = db_manager.get_all_patients()
except Exception as e:
    # 显示友好错误提示
    st.markdown(f'''
    <div class="alert alert-error">
        <strong>⚠️ 数据加载失败</strong>
        <p>{str(e)}</p>
        <button class="btn btn-secondary" onclick="location.reload()">🔄 重试</button>
    </div>
    ''', unsafe_allow_html=True)
    st.stop()
```

**特点**:
- ✅ 捕获所有数据库操作异常
- ✅ 显示详细的错误信息
- ✅ 提供一键重试按钮
- ✅ 停止页面渲染避免级联错误

### 2. 友好的错误提示

#### 错误提示结构

```html
<div class="alert alert-error">
    <strong>⚠️ 数据加载失败</strong>
    <p>具体的错误信息...</p>
    <p style="margin-top: 0.5rem; font-size: 0.875rem;">可能原因说明</p>
    <button class="btn btn-secondary" onclick="location.reload()">🔄 重试</button>
</div>
```

**视觉设计**:
- 🔴 红色边框和背景（alert-error样式）
- ⚠️ 醒目的警告图标
- 📝 清晰的错误描述
- 💡 可能的原因提示
- 🔄 可点击的重试按钮

### 3. 智能错误定位

系统在不同位置添加了针对性的错误处理：

| 位置 | 错误类型 | 提示信息 |
|------|---------|---------|
| Dashboard - 逾期患者 | get_overdue_patients() | "无法获取逾期患者信息" |
| Dashboard - 今日待回访 | get_due_patients() | "无法获取今日待回访患者" |
| Dashboard - 统计数据 | get_all_patients() | "无法获取患者数据" + 可能原因 |
| 数据查看页面 | get_all_patients() | "无法获取患者数据" |
| AI功能页面 | get_all_patients() | "无法获取患者数据" |
| 数据分析页面 | get_all_patients() | "无法获取患者数据" |

## 🎨 界面展示

### 正常状态 vs 错误状态

#### 正常加载流程
```
┌─────────────────────────────┐
│  骨架屏加载中...             │
└─────────────────────────────┘
         ↓
┌─────────────────────────────┐
│  📊 智能提醒看板              │
│  [指标卡片] [患者列表]       │
└─────────────────────────────┘
```

#### 错误状态
```
┌──────────────────────────────────────────┐
│  ⚠️ 数据加载失败                          │
│                                          │
│  无法获取患者数据：database is locked     │
│                                          │
│  可能原因：数据库文件损坏、权限不足        │
│  或数据库被锁定                            │
│                                          │
│  [🔄 重试]                                │
└──────────────────────────────────────────┘
```

## 🔧 技术实现

### 1. Dashboard页面错误处理

```python
# 位置：app.py 第241-267行

# ⚠️ 首先检查是否有过期未回访的患者（忘记回访）
try:
    overdue_patients = db_manager.get_overdue_patients()
except Exception as e:
    loading_placeholder.empty()
    st.markdown(f'''
    <div class="alert alert-error">
        <strong>⚠️ 数据加载失败</strong>
        <p>无法获取逾期患者信息：{str(e)}</p>
        <button class="btn btn-secondary" onclick="location.reload()">🔄 重试</button>
    </div>
    ''', unsafe_allow_html=True)
    st.stop()

# 获取今日待回访患者列表
try:
    due_patients = db_manager.get_due_patients()
except Exception as e:
    loading_placeholder.empty()
    st.markdown(f'''
    <div class="alert alert-error">
        <strong>⚠️ 数据加载失败</strong>
        <p>无法获取今日待回访患者：{str(e)}</p>
        <button class="btn btn-secondary" onclick="location.reload()">🔄 重试</button>
    </div>
    ''', unsafe_allow_html=True)
    st.stop()

# 获取所有患者数据用于统计
try:
    all_patients = db_manager.get_all_patients()
except Exception as e:
    loading_placeholder.empty()
    st.markdown(f'''
    <div class="alert alert-error">
        <strong>⚠️ 数据加载失败</strong>
        <p>无法获取患者数据：{str(e)}</p>
        <p style="margin-top: 0.5rem; font-size: 0.875rem;">
            可能原因：数据库文件损坏、权限不足或数据库被锁定
        </p>
        <button class="btn btn-secondary" onclick="location.reload()">🔄 重试</button>
    </div>
    ''', unsafe_allow_html=True)
    st.stop()
```

### 2. 数据查看页面错误处理

```python
# 位置：app.py 第823-835行

elif st.session_state.page == 'data':
    st.title("📋 患者数据查看")
    st.markdown("---")
    
    # 获取所有患者数据
    try:
        all_patients = db_manager.get_all_patients()
    except Exception as e:
        st.markdown(f'''
        <div class="alert alert-error">
            <strong>⚠️ 数据加载失败</strong>
            <p>无法获取患者数据：{str(e)}</p>
            <button class="btn btn-secondary" onclick="location.reload()">🔄 重试</button>
        </div>
        ''', unsafe_allow_html=True)
        st.stop()
    
    if all_patients:
        # 正常数据处理逻辑...
```

### 3. 关键函数说明

#### st.stop()
```python
st.stop()  # 立即停止页面渲染
```
**作用**: 
- 阻止后续代码执行
- 避免级联错误
- 节省资源

#### loading_placeholder.empty()
```python
loading_placeholder.empty()  # 清除骨架屏
```
**作用**:
- 移除加载动画
- 显示错误提示
- 避免界面混乱

#### onclick="location.reload()"
```html
<button onclick="location.reload()">🔄 重试</button>
```
**作用**:
- 刷新当前页面
- 重新加载数据
- 最简单的恢复方式

## 📊 常见错误场景

### 场景1: 数据库被锁定

**错误信息**:
```
database is locked
```

**原因**:
- 另一个进程正在写入数据库
- 之前的操作未正确关闭连接
- 并发访问冲突

**解决方案**:
1. 点击"重试"按钮
2. 等待几秒后重试
3. 检查是否有其他程序占用数据库

### 场景2: 数据库文件损坏

**错误信息**:
```
database disk image is malformed
```

**原因**:
- 突然断电或强制关机
- 磁盘空间不足
- 文件系统错误

**解决方案**:
1. 使用备份恢复数据库
2. 运行数据库修复工具
3. 联系技术支持

### 场景3: 权限不足

**错误信息**:
```
unable to open database file
Permission denied
```

**原因**:
- 文件权限设置不正确
- 用户无权访问数据库文件
- 文件被移动或删除

**解决方案**:
1. 检查文件权限
2. 确认文件路径正确
3. 以管理员身份运行

### 场景4: 磁盘空间不足

**错误信息**:
```
disk I/O error
no space left on device
```

**原因**:
- 磁盘已满
- 配额限制
- 临时文件过多

**解决方案**:
1. 清理磁盘空间
2. 删除不必要的文件
3. 扩展存储空间

## 💡 最佳实践

### 1. 分层错误处理

```python
# ✅ 好的做法：在每个数据源添加错误处理
try:
    data1 = db_manager.get_data1()
except Exception as e:
    handle_error("数据源1", e)

try:
    data2 = db_manager.get_data2()
except Exception as e:
    handle_error("数据源2", e)

# ❌ 避免：只在外层捕获
try:
    data1 = db_manager.get_data1()
    data2 = db_manager.get_data2()
except Exception as e:
    # 无法区分是哪个数据源出错
    handle_error("未知数据源", e)
```

### 2. 提供详细错误信息

```python
# ✅ 好的做法：包含上下文信息
except Exception as e:
    st.markdown(f'''
    <div class="alert alert-error">
        <strong>⚠️ 数据加载失败</strong>
        <p>模块：Dashboard - 逾期患者</p>
        <p>错误：{str(e)}</p>
        <p>时间：{datetime.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
    </div>
    ''')

# ❌ 避免：只显示通用消息
except Exception:
    st.error("出错了")
```

### 3. 记录错误日志

```python
import logging

logger = logging.getLogger(__name__)

try:
    patients = db_manager.get_all_patients()
except Exception as e:
    logger.error(f"Failed to load patients: {e}", exc_info=True)
    show_error_to_user(e)
```

### 4. 优雅降级

```python
# 如果主数据源失败，尝试备用方案
try:
    patients = db_manager.get_all_patients()
except Exception as e:
    logger.error(f"Primary data source failed: {e}")
    try:
        # 尝试从缓存读取
        patients = load_from_cache()
        st.warning("⚠️ 显示的是缓存数据，可能不是最新的")
    except:
        # 完全失败
        show_error_message()
```

## 🚀 扩展建议

### 短期优化（1-2周）

1. **错误日志记录**
   ```python
   import logging
   
   def log_error(module, error):
       logger.error(f"[{module}] {error}", exc_info=True)
   ```

2. **错误分类**
   ```python
   if "locked" in str(e).lower():
       error_type = "database_locked"
   elif "malformed" in str(e).lower():
       error_type = "database_corrupted"
   else:
       error_type = "unknown"
   ```

3. **自动重试机制**
   ```python
   import time
   
   for attempt in range(3):
       try:
           data = db_manager.get_data()
           break
       except Exception as e:
           if attempt < 2:
               time.sleep(1)  # 等待1秒后重试
           else:
               raise e
   ```

### 中期扩展（1-2月）

4. **错误报告系统**
   - 收集错误信息
   - 发送到服务器
   - 生成错误报告

5. **健康检查**
   ```python
   def check_database_health():
       try:
           db_manager.test_connection()
           return True
       except:
           return False
   ```

6. **数据恢复向导**
   - 检测数据库问题
   - 提供修复选项
   - 引导用户恢复

### 长期规划（3-6月）

7. **监控系统**
   - 实时监控错误率
   - 告警通知
   - 性能分析

8. **容错架构**
   - 数据库主从复制
   - 自动故障转移
   - 数据同步

## 📝 测试方法

### 手动测试

1. **模拟数据库锁定**
   ```python
   # 在另一个终端打开数据库并保持连接
   python -c "import sqlite3; conn = sqlite3.connect('patients.db'); input('Press Enter to close...')"
   ```

2. **模拟权限错误**
   ```bash
   # 修改文件权限
   chmod 000 patients.db
   ```

3. **模拟文件损坏**
   ```bash
   # 备份后破坏文件
   cp patients.db patients_backup.db
   echo "corrupted" > patients.db
   ```

### 自动化测试

```python
import pytest
from unittest.mock import patch

def test_database_error_handling():
    """测试数据库错误处理"""
    with patch('db_manager.get_all_patients') as mock_get:
        mock_get.side_effect = Exception("database is locked")
        
        # 触发页面加载
        # 验证是否显示错误提示
        # 验证是否调用st.stop()
```

## 🔗 相关资源

- [Streamlit Error Handling](https://docs.streamlit.io/library/advanced-features/error-handling)
- [Python Exception Handling](https://docs.python.org/3/tutorial/errors.html)
- [SQLite Error Codes](https://www.sqlite.org/rescode.html)
- [Best Practices for Error Messages](https://www.nngroup.com/articles/error-message-guidelines/)

---

**实施完成时间**: 2026-05-10  
**应用页面**: Dashboard、数据查看等核心页面  
**覆盖范围**: 所有数据库查询操作  
**维护状态**: ✅ 活跃维护
