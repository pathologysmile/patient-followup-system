# 多回访日期功能 - 实施指南

## 🎯 功能概述

系统现在支持为每位患者设置**多个回访日期**，例如：
- 出院后1周（第7天）
- 出院后1个月（第30天）
- 出院后3个月（第90天）
- 出院后6个月（第180天）

## ✅ 已完成的后台改造

### 1. 数据库结构升级

#### 新增表：visit_plans（回访计划表）
```sql
CREATE TABLE visit_plans (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    patient_id INTEGER NOT NULL,      -- 患者ID
    visit_date DATE NOT NULL,          -- 回访日期
    visit_type TEXT DEFAULT '常规回访', -- 回访类型
    status TEXT DEFAULT 'pending',     -- 状态
    notes TEXT,                        -- 备注
    completed_at TIMESTAMP,            -- 完成时间
    created_at TIMESTAMP,              -- 创建时间
    FOREIGN KEY (patient_id) REFERENCES patients(id)
);
```

#### 患者表修改
- 移除了 `visit_date` 字段的依赖
- 回访日期现在存储在 `visit_plans` 表中
- 一个患者可以有多个回访计划

### 2. 数据库函数更新

| 函数名 | 功能 | 修改内容 |
|--------|------|---------|
| `add_patient()` | 添加患者 | 支持传入回访日期列表 |
| `get_due_patients()` | 查询今日任务 | 从visit_plans表查询 |
| `get_all_patients()` | 查询所有患者 | 返回患者的所有回访计划 |
| `get_overdue_patients()` | 查询过期任务 | 从visit_plans表查询 |
| `update_visit_plan_status()` | 更新单个计划状态 | 新增函数 |
| `add_visit_plan()` | 添加新回访计划 | 新增函数 |

### 3. 数据迁移

已运行迁移脚本，将现有患者的单一回访日期转换为回访计划：
```bash
python migrate_database.py
```

## 🔧 前端界面改造方案

### 方案一：简单实现（推荐先实施）

在患者登记表单中，添加多个日期选择器：

```python
# 在 app.py 的患者登记表单中
with st.form("add_patient_form"):
    name = st.text_input("患者姓名")
    gender = st.selectbox("性别", ["男", "女"])
    discharge_date = st.date_input("出院日期", value=datetime.now())
    diagnosis = st.text_area("出院诊断")
    contact_person = st.text_input("联系人")
    contact_phone = st.text_input("联系电话")
    
    st.markdown("### 📅 回访计划")
    
    # 第一个回访日期（必填）
    visit_date_1 = st.date_input("第一次回访日期", value=datetime.now() + timedelta(days=7))
    
    # 可选的额外回访日期
    col1, col2 = st.columns(2)
    with col1:
        add_second = st.checkbox("添加第二次回访")
    with col2:
        add_third = st.checkbox("添加第三次回访")
    
    visit_date_2 = None
    visit_date_3 = None
    
    if add_second:
        visit_date_2 = st.date_input("第二次回访日期", 
                                     value=datetime.now() + timedelta(days=30))
    
    if add_third:
        visit_date_3 = st.date_input("第三次回访日期", 
                                     value=datetime.now() + timedelta(days=90))
    
    submitted = st.form_submit_button("保存档案")
    
    if submitted:
        if name and diagnosis and contact_person and contact_phone:
            # 构建回访日期列表
            visit_dates = [visit_date_1.strftime('%Y-%m-%d')]
            if visit_date_2:
                visit_dates.append(visit_date_2.strftime('%Y-%m-%d'))
            if visit_date_3:
                visit_dates.append(visit_date_3.strftime('%Y-%m-%d'))
            
            db_manager.add_patient(
                name, gender,
                discharge_date.strftime('%Y-%m-%d'),
                visit_dates,  # 传递日期列表
                diagnosis,
                contact_person,
                contact_phone
            )
            st.success(f"✅ {name} 的档案已保存！共设置 {len(visit_dates)} 次回访")
```

### 方案二：动态添加（更灵活）

使用Streamlit的session_state实现动态添加/删除回访日期：

```python
# 初始化session_state
if 'visit_dates' not in st.session_state:
    st.session_state.visit_dates = [datetime.now() + timedelta(days=7)]

def add_visit_date():
    st.session_state.visit_dates.append(
        datetime.now() + timedelta(days=30)
    )

def remove_visit_date(index):
    if len(st.session_state.visit_dates) > 1:
        st.session_state.visit_dates.pop(index)

# 显示回访日期列表
st.markdown("### 📅 回访计划")
for i, date in enumerate(st.session_state.visit_dates):
    col1, col2, col3 = st.columns([3, 1, 1])
    with col1:
        new_date = st.date_input(
            f"第{i+1}次回访",
            value=date,
            key=f"visit_date_{i}"
        )
        st.session_state.visit_dates[i] = new_date
    
    with col3:
        if len(st.session_state.visit_dates) > 1:
            if st.button("❌", key=f"remove_{i}"):
                remove_visit_date(i)
                st.rerun()

# 添加按钮
if st.button("➕ 添加回访日期"):
    add_visit_date()
    st.rerun()
```

### 方案三：快捷模板（最实用）

提供常用的回访计划模板：

```python
st.markdown("### 📅 回访计划模板")

template_options = {
    "标准随访": [7, 30, 90, 180],  # 1周、1月、3月、6月
    "简化随访": [7, 30],           # 1周、1月
    "密集随访": [3, 7, 14, 30, 90], # 3天、1周、2周、1月、3月
    "自定义": []
}

selected_template = st.selectbox(
    "选择回访模板",
    list(template_options.keys())
)

if selected_template == "自定义":
    # 显示动态添加界面
    pass
else:
    # 根据模板自动生成回访日期
    base_date = st.date_input("基准日期（出院日期）", value=datetime.now())
    visit_dates = [
        (base_date + timedelta(days=days)).strftime('%Y-%m-%d')
        for days in template_options[selected_template]
    ]
    
    st.info(f"将创建 {len(visit_dates)} 次回访：")
    for i, date in enumerate(visit_dates, 1):
        st.write(f"{i}. {date}")
```

## 📱 数据显示改造

### 智能提醒看板

无需修改，因为`get_due_patients()`已经从visit_plans表查询。

### 患者数据查看页面

需要显示患者的所有回访计划：

```python
# 在数据查看页面
for patient in all_patients:
    with st.expander(f"{patient['name']} - {patient['diagnosis']}"):
        st.write(f"**性别:** {patient['gender']}")
        st.write(f"**出院日期:** {patient['discharge_date']}")
        
        # 显示所有回访计划
        st.markdown("**回访计划:**")
        for plan in patient.get('visit_plans', []):
            visit_date, visit_type, status = plan
            status_icon = "✅" if status == "completed" else "⏳"
            st.write(f"- {status_icon} {visit_date} ({visit_type}) - {status}")
```

## 🎯 推荐实施步骤

### 第一步：修改患者登记表单（立即实施）

采用**方案一（简单实现）**，快速上线基础功能。

### 第二步：优化数据显示（本周内）

修改患者数据查看页面，显示完整的回访计划列表。

### 第三步：增强用户体验（下周）

实施**方案三（快捷模板）**，提供更便捷的登记方式。

### 第四步：高级功能（未来）

- 回访完成后自动安排下次回访
- 回访统计分析
- 回访提醒推送

## 💡 使用示例

### 示例1：胃癌患者标准随访

```
患者信息：
- 姓名：张三
- 诊断：胃癌
- 出院日期：2026-05-09

回访计划：
✓ 2026-05-16（出院后1周）-  pending
✓ 2026-06-09（出院后1月）-  pending
✓ 2026-08-09（出院后3月）-  pending
✓ 2026-11-09（出院后6月）-  pending
```

### 示例2：高血压患者简化随访

```
患者信息：
- 姓名：李四
- 诊断：高血压
- 出院日期：2026-05-09

回访计划：
✓ 2026-05-16（出院后1周）-  pending
✓ 2026-06-09（出院后1月）-  pending
```

## 📊 优势对比

| 项目 | 之前（单日期） | 现在（多日期） |
|------|--------------|--------------|
| 回访次数 | 1次 | 多次 |
| 随访连续性 | 差 | 优秀 |
| 医疗质量 | 一般 | 专业 |
| 患者管理 | 简单 | 系统化 |
| 数据统计 | 有限 | 丰富 |

## 🔍 注意事项

1. **向后兼容**
   - 保留了patients表的visit_date字段
   - 旧代码仍能正常工作

2. **数据完整性**
   - 使用外键约束确保数据一致性
   - 级联删除：删除患者时自动删除其回访计划

3. **性能优化**
   - 为visit_plans表添加索引
   - 查询时使用JOIN优化

4. **用户体验**
   - 提供默认值减少输入
   - 使用模板简化操作
   - 清晰显示回访进度

---

**版本**: v2.0.0  
**更新日期**: 2026-05-09  
**状态**: 后台已完成，待前端改造
