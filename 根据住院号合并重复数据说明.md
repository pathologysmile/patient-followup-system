# 根据住院号合并重复数据功能说明

## 功能概述

系统新增了**数据管理**页面，可以自动查找并合并具有相同住院号的重复患者记录。这对于处理CSV导入时产生的重复数据非常有用。

## 使用场景

### 常见重复数据来源

1. **CSV 批量导入**：多次导入同一批患者数据
2. **手动录入错误**：同一患者被多次登记
3. **系统迁移**：从旧系统导入数据时产生重复

### 示例场景

```
住院号: 202605051705

重复记录1:
  - 姓名: 张三
  - 出院日期: 2026-05-05
  - 回访计划: 2026-05-12, 2026-06-05
  - 创建时间: 2026-05-10 10:00:00

重复记录2:
  - 姓名: 张三
  - 出院日期: 2026-05-05
  - 回访计划: 2026-08-05
  - 创建时间: 2026-05-10 14:30:00

合并后:
  - 保留一条患者记录
  - 包含所有回访计划: 2026-05-12, 2026-06-05, 2026-08-05
```

## 使用方法

### 1. 访问数据管理页面

在左侧边栏点击 **"🔧 数据管理"** 按钮。

### 2. 扫描重复记录

点击 **"🔎 扫描重复记录"** 按钮，系统会自动查找所有具有相同住院号的患者记录。

### 3. 查看重复数据

如果发现重复记录，系统会显示：
- 重复的住院号
- 该住院号下的所有患者记录列表
- 每条记录的详细信息（ID、姓名、性别、出院日期、诊断等）

### 4. 选择要保留的记录

对于每个重复的住院号：
1. 查看表格中的所有记录
2. 在下拉菜单中选择要保留的记录
3. **建议**：选择创建时间最新或信息最完整的记录

### 5. 执行合并

点击 **"✅ 合并"** 按钮，系统会：
1. 将所有回访计划迁移到保留的记录
2. 删除其他重复的患者记录
3. 显示合并结果统计

## 界面示例

```
┌─────────────────────────────────────┐
│ 🔧 数据管理                          │
├─────────────────────────────────────┤
│ ⚠️ 警告：以下操作会修改数据库         │
├─────────────────────────────────────┤
│ 🔍 查找重复患者记录                  │
│ [🔎 扫描重复记录]                    │
├─────────────────────────────────────┤
│ ⚠️ 发现 2 个住院号存在重复记录       │
│                                     │
│ ▼ 📋 住院号: 202605051705 (2条记录) │
│   ┌────┬──────┬──────┬──────────┐  │
│   │ ID │ 姓名 │ 性别 │ 创建时间 │  │
│   ├────┼──────┼──────┼──────────┤  │
│   │ 5  │ 张三 │ 男   │ 10:00    │  │
│   │ 8  │ 张三 │ 男   │ 14:30    │  │
│   └────┴──────┴──────┴──────────┘  │
│                                     │
│ 选择要保留的记录：                    │
│ [张三 (ID: 8, 创建: 14:30) ▼]       │
│                                     │
│ [✅ 合并]                            │
├─────────────────────────────────────┤
│ 📊 数据库统计                        │
│ 总患者数: 98 | 待回访: 45           │
│ 已完成: 53 | 回访计划总数: 156      │
└─────────────────────────────────────┘
```

## 技术实现

### 后端函数（db_manager.py）

#### 1. `find_duplicate_patients_by_hospital_number()`

查找具有相同住院号的重复患者记录。

```python
def find_duplicate_patients_by_hospital_number():
    """
    查找具有相同住院号的重复患者记录
    返回: dict，key为住院号，value为该住院号对应的患者列表
    """
    # SQL查询：查找有重复住院号的患者
    SELECT hospital_number, COUNT(*) as count
    FROM patients
    WHERE hospital_number IS NOT NULL AND hospital_number != ''
    GROUP BY hospital_number
    HAVING count > 1
```

**返回格式：**
```python
{
    "202605051705": [
        {"id": 5, "name": "张三", "gender": "男", ...},
        {"id": 8, "name": "张三", "gender": "男", ...}
    ],
    "202605061234": [
        {"id": 12, "name": "李四", "gender": "女", ...},
        {"id": 15, "name": "李四", "gender": "女", ...}
    ]
}
```

#### 2. `merge_duplicate_patients(hospital_number, keep_patient_id)`

合并具有相同住院号的重复患者记录。

```python
def merge_duplicate_patients(hospital_number, keep_patient_id):
    """
    合并具有相同住院号的重复患者记录
    
    参数:
        hospital_number: 住院号
        keep_patient_id: 要保留的患者ID（主记录）
    
    返回:
        (merged_count, deleted_count, message)
    """
    # 步骤1: 获取该住院号下的所有患者
    # 步骤2: 迁移回访计划到保留的患者
    UPDATE visit_plans SET patient_id = ? WHERE patient_id = ?
    # 步骤3: 删除旧的患者记录
    DELETE FROM patients WHERE id = ?
```

**合并逻辑：**
1. 找出该住院号下的所有患者ID
2. 对于每个需要删除的患者：
   - 将其所有回访计划的 `patient_id` 更新为保留的患者ID
   - 删除该患者记录
3. 提交事务并返回统计信息

### 前端页面（app.py）

新增 **"🔧 数据管理"** 页面，包含：

1. **重复数据扫描**
   - 调用 `find_duplicate_patients_by_hospital_number()`
   - 显示所有重复的住院号和记录

2. **交互式合并**
   - 为每个重复住院号提供下拉选择框
   - 用户选择要保留的记录
   - 点击合并按钮执行操作

3. **数据库统计**
   - 显示总患者数、待回访数、已完成数
   - 显示回访计划总数

4. **备份提示**
   - 提醒用户定期备份数据库

## 合并流程

```
开始
  ↓
扫描重复记录
  ↓
发现重复？──否──→ 显示"无重复记录"
  ↓是
显示重复列表
  ↓
用户选择保留记录
  ↓
确认合并
  ↓
迁移回访计划
  ↓
删除重复记录
  ↓
提交事务
  ↓
显示结果
  ↓
结束
```

## 数据安全

### ✅ 安全措施

1. **事务保护**：使用数据库事务，失败时自动回滚
2. **外键约束**：`visit_plans` 表的外键确保数据完整性
3. **明确提示**：操作前显示警告信息
4. **详细日志**：显示合并的详细统计信息

### ⚠️ 注意事项

1. **操作不可逆**：合并后删除的记录无法恢复
2. **建议备份**：操作前先备份 `patients.db` 文件
3. **仔细检查**：确认选择正确的保留记录
4. **测试环境**：建议先在测试环境验证

## 常见问题

### Q1: 合并后会丢失数据吗？

A: 不会。合并只会删除重复的患者记录，但会将所有回访计划迁移到保留的记录中，确保数据不丢失。

### Q2: 如果两条记录的信息不同怎么办？

A: 系统会保留您选择的记录的所有信息（姓名、性别、诊断等），只合并回访计划。建议在合并前检查哪条记录的信息更完整。

### Q3: 可以撤销合并操作吗？

A: 不可以。合并操作会直接修改数据库，删除的记录无法恢复。请务必在操作前备份数据库。

### Q4: 如果没有住院号怎么办？

A: 系统只会查找有住院号且住院号不为空的记录。没有住院号的记录不会被检测为重复。

### Q5: 合并后回访计划的状态会改变吗？

A: 不会。回访计划的状态（pending/completed）和完成时间都会保持不变。

## 最佳实践

### 1. 定期检查和合并

建议每周或每月检查一次重复记录，保持数据整洁。

### 2. 操作前备份

```bash
# Windows PowerShell
Copy-Item patients.db patients_backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').db
```

### 3. 先预览再操作

1. 点击"扫描重复记录"查看有哪些重复
2. 仔细检查每条记录的信息
3. 选择合适的保留记录
4. 确认无误后再点击"合并"

### 4. 验证合并结果

合并后检查：
- 患者总数是否减少
- 回访计划总数是否保持不变
- 保留的记录是否包含所有回访计划

## 相关文件

- [db_manager.py](file:///D:/remindercode/db_manager.py#L288-L413) - 后端函数实现
- [app.py](file:///D:/remindercode/app.py#L472-L569) - 前端页面实现

## 扩展功能（未来计划）

1. **自动合并**：根据规则自动选择保留记录（如最新创建的）
2. **合并历史**：记录所有合并操作的日志
3. **批量合并**：一键合并所有重复记录
4. **冲突检测**：检测信息不一致的记录并提示用户
5. **恢复功能**：提供合并后的恢复选项
