# 住院号功能说明

## 功能概述

在患者出院登记系统中新增了"住院号"字段，用于记录患者的住院编号，方便医院内部管理和追踪。

## 更新内容

### 1. 数据库结构更新

在 `patients` 表中添加了 `hospital_number` 字段（TEXT类型，可选）：

```sql
ALTER TABLE patients ADD COLUMN hospital_number TEXT
```

### 2. 代码更新

#### db_manager.py
- ✅ `add_patient()` 函数新增 `hospital_number` 参数（可选，默认为 None）
- ✅ `get_all_patients()` 函数返回数据中包含 `hospital_number` 字段
- ✅ `import_patients_from_csv()` 函数支持从 CSV 导入住院号（列名："住院号"）

#### app.py
- ✅ 新患者登记表单新增"住院号"输入框（位于性别和出院日期之间）
- ✅ 数据查看页面表格显示"住院号"列
- ✅ 患者详情信息显示住院号（如果有）

### 3. 数据库迁移

提供了迁移脚本 `migrate_add_hospital_number.py`，用于为现有数据库添加住院号字段：

```bash
python migrate_add_hospital_number.py
```

## 使用方法

### 方式一：手动登记

1. 在左侧边栏的"📝 新患者出院登记"表单中
2. 填写患者信息时，在"住院号"字段输入住院编号（可选）
3. 点击"保存档案"即可

### 方式二：CSV 批量导入

CSV 文件格式示例：

```csv
姓名,性别,住院号,出院日期,回访日期,诊断,联系人,联系电话
张三,男,ZY20260510001,2026-05-10,2026-05-17,高血压,李四,13800138000
王五,女,,2026-05-10,2026-05-17,糖尿病,赵六,13900139000
```

**注意：**
- "住院号"列为可选，可以为空
- 列名必须为"住院号"（区分大小写）

## 功能特点

1. **可选字段**：住院号为可选项，不填写不影响患者档案创建
2. **向后兼容**：旧数据会自动将住院号设为 NULL
3. **灵活查询**：可在数据查看页面查看所有患者的住院号
4. **批量导入**：支持通过 CSV 文件批量导入带住院号的患者数据

## 测试验证

运行测试脚本验证功能：

```bash
python test_hospital_number.py
```

预期输出：
```
============================================================
测试住院号功能
============================================================

【测试1】添加带住院号的患者...
✅ 成功添加患者A（住院号: ZY20260510001）

【测试2】添加不带住院号的患者...
✅ 成功添加患者B（无住院号）

【测试3】查询所有患者...
  - 测试患者A: 住院号=ZY20260510001
  - 测试患者B: 住院号=None

============================================================
测试完成！
============================================================
```

## 注意事项

1. 如果数据库中已有旧数据，需要先运行迁移脚本添加字段
2. CSV 导入时，住院号列可以为空，系统会自动处理为 NULL
3. 住院号没有格式限制，可以根据医院实际情况填写（如：ZY20260510001、2026-H001 等）

## 相关文件

- `db_manager.py` - 数据库管理模块
- `app.py` - Streamlit 界面应用
- `migrate_add_hospital_number.py` - 数据库迁移脚本
- `test_hospital_number.py` - 功能测试脚本
