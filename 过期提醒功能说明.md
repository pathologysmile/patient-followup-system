# 过期未回访提醒功能说明

## 🎯 功能概述

系统现在会**第一时间检测并提示**已过回访日期但状态仍为`pending`的患者，防止医护人员忘记回访。

## ⚠️ 提醒级别

系统采用三级提醒机制，按优先级从高到低显示：

### 🔴 第一级：过期未回访（最紧急）
- **触发条件**：回访日期 < 今天 且 状态 = pending
- **显示位置**：智能提醒看板顶部
- **提醒样式**：红色错误框 + 🚨 警告图标
- **示例**：
  ```
  🚨 紧急提醒：发现 2 个患者已过回访日期但未回访！
  ⏰ 以下患者已错过回访时间，请立即处理：
  
  🚨 逾期 3 天 - 张三 (男, 胃癌)
  🚨 逾期 1 天 - 李四 (女, 糖尿病)
  ```

### 🟠 第二级：今日紧急
- **触发条件**：回访日期 = 今天 且 状态 = pending
- **显示位置**：过期提醒之后
- **提醒样式**：红色错误框
- **示例**：
  ```
  ⚠️ 发现 2 个【今日】紧急回访任务！请立即处理。
  ```

### 🟡 第三级：明日待办
- **触发条件**：回访日期 = 明天 且 状态 = pending
- **显示位置**：今日任务之后
- **提醒样式**：普通展开框
- **示例**：
  ```
  明日待办 - 王五 (男, 高血压)
  ```

## 📋 过期患者卡片功能

每个过期未回访的患者卡片包含：

### 左侧信息区
1. **回访日期**（红色高亮）
2. **逾期天数**（红色高亮）
3. **联系人**（如有）
4. **联系电话**（如有）

### 快速操作按钮
1. **✅ 立即标记为已完成**
   - 点击后直接将状态改为 completed
   - 适用于已经电话回访但忘记更新状态的情况

2. **📅 修改回访日期**
   - 点击后显示日期选择器
   - 可以重新安排回访时间
   - 适用于需要延后回访的情况

3. **✨ 生成话术**
   - AI会根据"逾期X天"的状态生成适合的话术
   - 话术会以黄色警告框显示（区别于正常的绿色框）

### 右侧话术区
- 显示AI生成的回访建议话术
- 逾期患者的话术会更加委婉和道歉性

## 💡 使用场景

### 场景1：昨天应该回访但忘记了
```
早上打开系统 → 看到红色警告"逾期 1 天"
→ 点击"生成话术"获取道歉性话术
→ 拨打电话进行回访
→ 点击"立即标记为已完成"
```

### 场景2：患者要求延后回访
```
看到逾期提醒 → 联系患者确认
→ 患者希望改到下周
→ 点击"修改回访日期"
→ 选择新的日期（如下周一）
→ 点击"保存"
→ 该患者从过期列表移除，出现在未来的待办中
```

### 场景3：已经回访但忘记更新状态
```
看到逾期提醒 → 想起昨天已经打过电话
→ 直接点击"立即标记为已完成"
→ 状态更新，提醒消失
```

## 🔧 技术实现

### 数据库查询
```python
def get_overdue_patients():
    """查询已过回访日期但状态仍为pending的患者"""
    today = datetime.now().strftime('%Y-%m-%d')
    
    SELECT id, name, gender, diagnosis, visit_date, 
           contact_person, contact_phone, created_at
    FROM patients 
    WHERE visit_date < ? AND status = 'pending'
    ORDER BY visit_date ASC
```

### 逾期天数计算
```python
days_overdue = (datetime.now() - datetime.strptime(visit_date, '%Y-%m-%d')).days
```

### 前端显示逻辑
```python
# 1. 首先检查过期患者
overdue_patients = db_manager.get_overdue_patients()
if overdue_patients:
    # 显示红色警告
    st.error("🚨 紧急提醒...")
    
    # 遍历显示每个过期患者
    for patient in overdue_patients:
        # 显示详细信息和操作按钮

# 2. 然后显示今日和明日任务
due_patients = db_manager.get_due_patients()
```

## 📊 显示顺序

智能提醒看板的显示顺序（从上到下）：

```
┌─────────────────────────────────────┐
│ 1. 🚨 过期未回访（最优先）          │
│    - 逾期3天的患者                  │
│    - 逾期2天的患者                  │
│    - 逾期1天的患者                  │
├─────────────────────────────────────┤
│ 2. ⚠️ 今日紧急任务                  │
│    - 今天需要回访的患者             │
├─────────────────────────────────────┤
│ 3. 🟡 明日待办任务                  │
│    - 明天需要回访的患者             │
└─────────────────────────────────────┘
```

## ✨ 功能优势

1. **第一时间发现**
   - 打开系统就能看到过期提醒
   - 不会被其他信息淹没

2. **分级提醒**
   - 过期 > 今日 > 明日
   - 优先级清晰

3. **快速处理**
   - 一键标记完成
   - 一键修改日期
   - 无需跳转到其他页面

4. **智能话术**
   - 针对逾期情况生成合适的话术
   - 更加人性化和专业

5. **防止遗漏**
   - 只要状态是pending且日期已过
   - 就会一直显示在顶部
   - 直到处理完成

## 🎨 视觉设计

### 颜色方案
- **红色** (#FF4B4B)：过期警告、今日紧急
- **黄色** (#FFA500)：逾期话术、明日待办
- **绿色** (#00CC00)：正常话术、已完成

### 图标使用
- 🚨：过期未回访（最紧急）
- ⚠️：今日紧急
- ✅：完成操作
- 📅：日期修改
- ✨：生成话术

## 📝 最佳实践

1. **每天早上第一件事**
   - 打开系统查看智能提醒看板
   - 优先处理顶部的过期患者

2. **及时处理**
   - 看到过期提醒立即行动
   - 不要拖延，避免逾期天数增加

3. **定期清理**
   - 每周检查一次是否有长期逾期的患者
   - 考虑是否需要调整回访策略

4. **合理设置回访日期**
   - 出院时与患者确认方便的回访时间
   - 避免设置在不合适的日期

## 🔍 常见问题

### Q1: 为什么有些患者会出现在过期列表中？
**A:** 可能的原因：
- 回访日期已过但未进行回访
- 已经回访但忘记更新状态
- 患者要求延后但未修改日期

### Q2: 如何避免患者进入过期列表？
**A:** 
- 每天查看"今日紧急"任务并及时处理
- 回访完成后立即标记为"已完成"
- 如需延后，及时修改回访日期

### Q3: 逾期很多天的患者怎么办？
**A:** 
- 优先联系患者，表达歉意
- 使用AI生成的逾期话术
- 尽快安排回访
- 考虑是否需要上级介入

### Q4: 可以关闭这个提醒吗？
**A:** 
不建议关闭，这是重要的质量控制功能。
如确实不需要，可以注释掉相关代码。

---

**版本**: v1.1.0  
**更新日期**: 2026-05-09  
**功能状态**: ✅ 已启用
