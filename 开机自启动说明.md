# 患者回访系统 - 开机自启动配置说明

## 📋 功能说明

配置后，系统在以下情况会自动启动：
- ✅ 电脑开机并登录用户账户时
- ✅ 自动在后台启动 Streamlit 服务
- ✅ 自动打开浏览器显示提醒页面（http://localhost:8501）

## 🚀 快速开始

### 方法一：使用自动化脚本（推荐）

1. **右键点击** `install_autostart.bat`
2. 选择 **"以管理员身份运行"**
3. 按照提示完成配置
4. 重启电脑测试效果

### 方法二：手动配置任务计划

1. 按 `Win + R`，输入 `taskschd.msc`，回车
2. 点击右侧 **"创建任务"**
3. 填写以下信息：
   - **名称**：患者回访系统自动启动
   - **描述**：开机自动启动患者回访智能提醒系统

4. **触发器**选项卡：
   - 点击"新建"
   - 选择"登录时"
   - 延迟任务时间：10秒
   - 点击"确定"

5. **操作**选项卡：
   - 点击"新建"
   - 程序或脚本：`D:\remindercode\auto_start.bat`
   - 起始于：`D:\remindercode`
   - 点击"确定"

6. **设置**选项卡：
   - 取消勾选"只有在计算机使用交流电源时才启动此任务"
   - 点击"确定"

## 📁 文件说明

| 文件名 | 用途 |
|--------|------|
| `auto_start.bat` | Windows批处理启动脚本 |
| `auto_start.ps1` | PowerShell启动脚本（备用） |
| `auto_start_task.xml` | 任务计划配置文件 |
| `install_autostart.bat` | 一键安装开机自启动 |
| `uninstall_autostart.bat` | 取消开机自启动 |

## ⚙️ 高级配置

### 修改启动延迟时间

编辑 `auto_start_task.xml` 文件，找到：
```xml
<Delay>PT10S</Delay>
```
改为需要的延迟时间，例如：
- `PT5S` = 5秒
- `PT30S` = 30秒
- `PT1M` = 1分钟

### 修改浏览器打开的页面

编辑 `auto_start.bat` 文件，找到：
```bat
start http://localhost:8501
```
改为其他URL，例如：
```bat
start http://localhost:8501/?page=data
```

### 禁止电池模式下启动

编辑 `auto_start_task.xml`，将：
```xml
<DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
```
改为：
```xml
<DisallowStartIfOnBatteries>true</DisallowStartIfOnBatteries>
```

## 🔧 常见问题

### Q1: 启动后浏览器没有自动打开？
**A:** 可能是服务器启动较慢，增加 `auto_start.bat` 中的等待时间：
```bat
timeout /t 5 /nobreak >nul
```

### Q2: 如何手动停止自动启动的服务？
**A:** 
1. 打开任务管理器
2. 找到 `streamlit.exe` 进程
3. 右键结束进程

或者在命令行运行：
```powershell
taskkill /F /IM streamlit.exe
```

### Q3: 如何查看任务计划是否成功创建？
**A:** 
1. 按 `Win + R`，输入 `taskschd.msc`
2. 在左侧找到"任务计划程序库"
3. 查找"患者回访系统自动启动"

### Q4: 开机后想临时禁止自动启动怎么办？
**A:** 运行 `uninstall_autostart.bat` 取消自启动，需要时再重新安装。

### Q5: 虚拟环境路径不同怎么办？
**A:** 编辑 `auto_start.bat`，修改虚拟环境路径：
```bat
call D:\remindercode\.venv\Scripts\activate.bat
```
改为你的实际路径。

## 🛡️ 安全提示

- 脚本需要管理员权限才能创建任务计划
- 建议定期更新系统和Python依赖包
- 不要随意修改脚本内容，除非了解其作用

## 📞 技术支持

如遇到问题，请检查：
1. Python和虚拟环境是否正常安装
2. 项目路径是否正确
3. 是否有防火墙阻止了8501端口
4. 任务计划是否成功创建

---
**版本**: 1.0  
**更新日期**: 2026-05-09
