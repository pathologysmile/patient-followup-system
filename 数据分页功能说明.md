# 数据分页功能说明

## 📋 功能概述

系统已实现**智能数据分页**功能，优化大量数据的加载和显示性能，提升用户体验。

## ✨ 核心特性

### 1. 自动分页检测

```python
# 智能判断是否需要分页
items_per_page = 20  # 每页显示条数
total_items = len(filtered_df)
total_pages = max(1, (total_items + items_per_page - 1) // items_per_page)

if total_items > items_per_page:
    # 显示分页控件
else:
    # 直接显示所有数据
```

**特点**:
- ✅ 数据量≤20条时不显示分页
- ✅ 数据量>20条时自动启用分页
- ✅ 动态计算总页数（向上取整）

### 2. 多种导航方式

#### 2.1 下拉选择器
```python
page_options = [f"第 {i} 页" for i in range(1, total_pages + 1)]
selected_page_label = st.selectbox("选择页码", page_options)
```

**优势**:
- 直观显示所有页码
- 快速跳转到任意页
- 适合页数较少（<50页）的场景

#### 2.2 数字输入框
```python
jump_page = st.number_input(
    "跳转到",
    min_value=1,
    max_value=total_pages,
    value=current_page,
    step=1
)
```

**优势**:
- 精确输入页码
- 支持键盘输入
- 适合页数较多的场景

### 3. 实时状态显示

```python
# 顶部信息
st.markdown(f"**共 {total_items} 条记录**")
st.markdown(f"**共 {total_pages} 页**")

# 底部提示
st.caption(f"📄 显示第 {start_idx + 1}-{end_idx} 条，共 {total_items} 条")
```

**显示内容**:
- 总记录数
- 总页数
- 当前页范围
- 当前位置提示

### 4. 数据切片优化

```python
# 计算当前页的数据范围
start_idx = (current_page - 1) * items_per_page
end_idx = min(start_idx + items_per_page, total_items)

# 只获取当前页的数据
page_df = filtered_df.iloc[start_idx:end_idx]
```

**性能优势**:
- ✅ 只渲染当前页数据
- ✅ 减少DOM节点数量
- ✅ 降低内存占用
- ✅ 提升响应速度

## 🎨 界面布局

### 分页控件布局

```
┌─────────────────────────────────────────────────────┐
│  [共 X 条记录] [页码下拉框] [共 Y 页] [跳转输入框]   │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│  📄 显示第 1-20 条，共 100 条                        │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│                  数据表格                             │
│  （只显示当前页的20条数据）                           │
└─────────────────────────────────────────────────────┘
```

### 列宽比例

```python
col_page1, col_page2, col_page3, col_page4 = st.columns([1, 2, 1, 2])
#     总计       页码选择      总页数      快速跳转
#     1份        2份          1份        2份
```

**设计理由**:
- 页码选择和跳转需要更多空间
- 统计信息简洁明了
- 视觉平衡美观

## 🔧 技术实现

### 完整代码示例

```python
# ===== 分页控制 =====
items_per_page = 20  # 每页显示条数
total_items = len(filtered_df)
total_pages = max(1, (total_items + items_per_page - 1) // items_per_page)

if total_items > items_per_page:
    col_page1, col_page2, col_page3, col_page4 = st.columns([1, 2, 1, 2])
    
    with col_page1:
        st.markdown(f"**共 {total_items} 条记录**")
    
    with col_page2:
        # 页码选择器
        page_options = [f"第 {i} 页" for i in range(1, total_pages + 1)]
        selected_page_label = st.selectbox(
            "选择页码",
            page_options,
            index=0,
            label_visibility="collapsed"
        )
        current_page = int(selected_page_label.replace("第 ", "").replace(" 页", ""))
    
    with col_page3:
        st.markdown(f"**共 {total_pages} 页**")
    
    with col_page4:
        # 快速跳转
        jump_page = st.number_input(
            "跳转到",
            min_value=1,
            max_value=total_pages,
            value=current_page,
            step=1,
            label_visibility="collapsed"
        )
        if jump_page != current_page:
            current_page = jump_page
            st.rerun()
    
    # 计算当前页的数据范围
    start_idx = (current_page - 1) * items_per_page
    end_idx = min(start_idx + items_per_page, total_items)
    page_df = filtered_df.iloc[start_idx:end_idx]
    
    st.caption(f"📄 显示第 {start_idx + 1}-{end_idx} 条，共 {total_items} 条")
else:
    page_df = filtered_df
    current_page = 1

# 显示数据表格（使用分页后的数据）
st.dataframe(page_df[columns], use_container_width=True)
```

### 关键逻辑说明

#### 1. 页数计算（向上取整）

```python
# 方法1: 数学公式
total_pages = (total_items + items_per_page - 1) // items_per_page

# 示例:
# 100条数据，每页20条 → (100 + 20 - 1) // 20 = 119 // 20 = 5页
# 101条数据，每页20条 → (101 + 20 - 1) // 20 = 120 // 20 = 6页

# 方法2: 使用math.ceil
import math
total_pages = math.ceil(total_items / items_per_page)
```

#### 2. 数据切片边界保护

```python
# 确保不超过数据总量
end_idx = min(start_idx + items_per_page, total_items)

# 示例:
# 第5页（最后页），总共101条
# start_idx = (5-1) * 20 = 80
# end_idx = min(80 + 20, 101) = min(100, 101) = 100
# 实际显示: 第81-100条（20条）

# 如果有105条
# end_idx = min(80 + 20, 105) = min(100, 105) = 100
# 还需要最后一页显示剩余的5条
```

#### 3. 页码解析

```python
# 从"第 3 页"提取数字3
selected_page_label = "第 3 页"
current_page = int(selected_page_label.replace("第 ", "").replace(" 页", ""))
# 结果: current_page = 3
```

## 📊 应用场景

### 场景1: 患者数据查看

**数据量**: 100-1000条  
**分页设置**: 每页20条  
**效果**: 
- 100条 → 5页
- 500条 → 25页
- 1000条 → 50页

**用户体验**:
- ✅ 快速浏览患者列表
- ✅ 轻松定位特定患者
- ✅ 避免长滚动条

### 场景2: 回访记录查询

**数据量**: 500-5000条  
**分页设置**: 每页50条（可调整）  
**效果**:
- 500条 → 10页
- 5000条 → 100页

**优化建议**:
- 增加每页显示数量
- 添加搜索过滤
- 提供导出功能

### 场景3: 数据分析报表

**数据量**: 不确定  
**分页策略**: 动态调整

```python
# 根据数据量自动调整每页条数
if total_items < 50:
    items_per_page = 10
elif total_items < 500:
    items_per_page = 50
else:
    items_per_page = 100
```

## 💡 最佳实践

### 1. 选择合适的每页条数

| 数据类型 | 推荐每页条数 | 原因 |
|---------|------------|------|
| 简单列表 | 20-30条 | 快速浏览 |
| 详细表格 | 10-20条 | 信息密集 |
| 卡片视图 | 12-24条 | 网格布局 |
| 图表数据 | 50-100条 | 批量分析 |

### 2. 保持页码一致性

```python
# ✅ 好的做法：固定每页条数
ITEMS_PER_PAGE = 20  # 常量定义

# ❌ 避免：动态变化导致困惑
items_per_page = random.choice([10, 20, 50])
```

### 3. 记住用户选择

```python
# 使用session_state保存用户的分页偏好
if 'page_preference' not in st.session_state:
    st.session_state.page_preference = 20

items_per_page = st.session_state.page_preference
```

### 4. 提供快捷操作

```python
# 首页/末页按钮
col_first, col_prev, col_next, col_last = st.columns(4)

with col_first:
    if st.button("⏮ 首页"):
        current_page = 1
        st.rerun()

with col_last:
    if st.button("末页 ⏭"):
        current_page = total_pages
        st.rerun()
```

### 5. 处理空数据

```python
if total_items == 0:
    st.info("📭 暂无数据")
    # 不显示分页控件
else:
    # 显示分页和数据
```

## ⚡ 性能优化

### 1. 延迟加载

```python
# 只在切换页面时加载数据
@st.cache_data
def get_page_data(page_number, items_per_page):
    # 从数据库或API获取指定页的数据
    start_idx = (page_number - 1) * items_per_page
    end_idx = start_idx + items_per_page
    return fetch_data_from_db(start_idx, end_idx)
```

### 2. 虚拟滚动（高级）

对于超大数据集（>10000条），考虑使用虚拟滚动：

```python
# 只渲染可视区域的数据
visible_range = calculate_visible_range(scroll_position)
visible_data = all_data[visible_range.start:visible_range.end]
```

### 3. 数据预取

```python
# 预取下一页数据
next_page = current_page + 1
if next_page <= total_pages:
    preload_data(next_page)
```

## 🎯 扩展功能

### 1. 自定义每页条数

```python
items_per_page = st.selectbox(
    "每页显示",
    [10, 20, 50, 100],
    index=1  # 默认20
)
```

### 2. 页码导航按钮

```python
# 上一页/下一页
col_prev, col_next = st.columns(2)

with col_prev:
    if current_page > 1:
        if st.button("← 上一页"):
            current_page -= 1
            st.rerun()

with col_next:
    if current_page < total_pages:
        if st.button("下一页 →"):
            current_page += 1
            st.rerun()
```

### 3. 页码范围显示

```python
# 当页数很多时，显示页码范围
if total_pages > 10:
    start_page = max(1, current_page - 2)
    end_page = min(total_pages, current_page + 2)
    
    for page in range(start_page, end_page + 1):
        if st.button(f"{page}", key=f"page_{page}"):
            current_page = page
            st.rerun()
```

### 4.  goTo 功能增强

```python
# 支持直接输入页码并回车跳转
jump_page = st.text_input("跳转到页码", value=str(current_page))
if jump_page.isdigit():
    page_num = int(jump_page)
    if 1 <= page_num <= total_pages and page_num != current_page:
        current_page = page_num
        st.rerun()
```

## 📝 使用示例

### 示例1: 基础分页

```python
# 在数据查看页面
filtered_df = apply_filters(df)  # 应用筛选

# 添加分页
items_per_page = 20
total_pages = (len(filtered_df) + items_per_page - 1) // items_per_page

if len(filtered_df) > items_per_page:
    page = st.selectbox("页码", range(1, total_pages + 1))
    start_idx = (page - 1) * items_per_page
    page_df = filtered_df.iloc[start_idx:start_idx + items_per_page]
else:
    page_df = filtered_df

# 显示数据
st.dataframe(page_df)
```

### 示例2: 带搜索的分页

```python
# 先搜索，再分页
search_result = df[df['name'].str.contains(keyword)]

# 对搜索结果分页
items_per_page = 20
total_pages = (len(search_result) + items_per_page - 1) // items_per_page

if len(search_result) > items_per_page:
    page = st.selectbox("页码", range(1, total_pages + 1))
    start_idx = (page - 1) * items_per_page
    page_df = search_result.iloc[start_idx:start_idx + items_per_page]
else:
    page_df = search_result

st.dataframe(page_df)
```

### 示例3: 多条件筛选+分页

```python
# 多重筛选
filtered_df = df.copy()
if status_filter != "全部":
    filtered_df = filtered_df[filtered_df['status'] == status_filter]
if diagnosis_filter != "全部":
    filtered_df = filtered_df[filtered_df['diagnosis'] == diagnosis_filter]

# 分页
items_per_page = 20
total_pages = (len(filtered_df) + items_per_page - 1) // items_per_page

if len(filtered_df) > items_per_page:
    # 显示分页控件
    ...
```

## 🔗 相关资源

- [Streamlit Pagination Examples](https://docs.streamlit.io/library/api-reference/data/st.dataframe)
- [Pandas iloc Documentation](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.iloc.html)
- [Data Table Best Practices](https://www.nngroup.com/articles/tables-displaying-comparative-data/)

---

**实施完成时间**: 2026-05-10  
**应用页面**: 患者数据查看页面  
**默认每页条数**: 20条  
**维护状态**: ✅ 活跃维护
