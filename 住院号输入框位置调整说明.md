# 住院号输入框位置调整说明

## 修改内容

在 Streamlit 页面的"📝 新患者出院登记"表单中，已将**住院号输入框**移动到**姓名输入框前面**。

## 修改前布局

```
┌─────────────────────────────────┐
│ 患者姓名                         │
├──────────────┬──────────────────┤
│ 性别         │ 住院号           │
├──────────────┼──────────────────┤
│ 出院日期     │ 计划回访日期     │
├──────────────┴──────────────────┤
│ 出院诊断                        │
├──────────────┬──────────────────┤
│ 联系人       │ 联系电话         │
└──────────────┴──────────────────┘
```

## 修改后布局

```
┌──────────────┬──────────────────┐
│ 住院号       │ 患者姓名         │
├──────────────┼──────────────────┤
│ 性别         │ 出院日期         │
├──────────────┼──────────────────┤
│ 计划回访日期 │ 出院诊断         │
├──────────────┬──────────────────┤
│ 联系人       │ 联系电话         │
└──────────────┴──────────────────┘
```

## 代码变更

### 文件：app.py（第88-111行）

**修改前：**
```python
with st.form("add_patient_form"):
    name = st.text_input("患者姓名")
    
    col1, col2 = st.columns(2)
    with col1:
        gender = st.selectbox("性别", ["男", "女"])
    with col2:
        hospital_number = st.text_input("住院号")
```

**修改后：**
```python
with st.form("add_patient_form"):
    col0, col1 = st.columns(2)
    with col0:
        hospital_number = st.text_input("住院号")
    with col1:
        name = st.text_input("患者姓名")
    
    col2, col3 = st.columns(2)
    with col2:
        gender = st.selectbox("性别", ["男", "女"])
    with col3:
        discharge_date = st.date_input("出院日期", value=datetime.now())
```

## 优势

1. **更符合业务流程**：住院号通常是患者的首要标识，放在前面更合理
2. **与 CSV 导入格式一致**：CSV 文件中住院号也在姓名列之前
3. **界面更清晰**：两列布局更加紧凑和美观

## 使用说明

1. 打开系统：[http://localhost:8501](http://localhost:8501)
2. 在左侧边栏找到"📝 新患者出院登记"区域
3. 填写表单时，**住院号**输入框现在位于第一行左侧
4. 住院号为可选项，不填写也可以保存档案

## 相关文件

- `app.py` - Streamlit 主应用文件（第88-111行）
- `db_manager.py` - 数据库管理模块（已支持住院号字段）
