# 源代码TXT文件快速使用指南

## 🎯 快速开始

### 打开文件
1. 双击 `project_source_code.txt` 文件
2. 或使用文本编辑器打开（推荐 VS Code、Notepad++、Sublime Text）

### 查找特定文件
按 `Ctrl + F` 搜索文件名，例如：
- 搜索 `app.py` - 主应用程序
- 搜索 `db_manager.py` - 数据库管理
- 搜索 `ai_agent.py` - AI智能体

## 📑 文件结构导航

### 主要模块位置（按重要性排序）

| 序号 | 文件名 | 说明 | 搜索关键词 |
|------|--------|------|-----------|
| 1 | app.py | Streamlit主界面 | `文件 1: .\app.py` |
| 2 | db_manager.py | 数据库操作 | `文件 2: .\db_manager.py` |
| 3 | ui_styles.py | UI样式定义 | `文件 3: .\ui_styles.py` |
| 4 | ai_agent.py | AI对话功能 | `文件 4: .\ai_agent.py` |
| 5 | main.py | 程序入口 | `文件 5: .\main.py` |

### 配置文件位置

| 文件名 | 说明 | 搜索关键词 |
|--------|------|-----------|
| requirements.txt | Python依赖包 | `文件 110: .\requirements.txt` |
| .env.example | 环境变量示例 | `.env.example` |
| metadata.json | 项目元数据 | `metadata.json` |

### 重要脚本位置

| 文件名 | 说明 | 搜索关键词 |
|--------|------|-----------|
| auto_start.bat | 开机自启动 | `auto_start.bat` |
| backup.bat | 数据备份 | `backup.bat` |
| deploy.bat | 部署脚本 | `deploy.bat` |

## 🔍 常用搜索技巧

### 1. 查找函数定义
```
搜索: "def " + 函数名
例如: def add_patient(
```

### 2. 查找类定义
```
搜索: "class " + 类名
例如: class DatabaseManager
```

### 3. 查找导入语句
```
搜索: "import " + 模块名
例如: import streamlit as st
```

### 4. 查找注释
```
搜索: "# " + 关键词
例如: # 患者信息
```

## 💡 实用场景

### 场景1: 代码审查
1. 从头到尾浏览主要文件
2. 关注核心业务逻辑
3. 检查代码规范和注释

### 场景2: 学习项目结构
1. 先查看 app.py 了解整体架构
2. 再查看 db_manager.py 了解数据层
3. 最后查看 ai_agent.py 了解AI功能

### 场景3: 版权申请准备
1. 提取前30页代码（约前9000行）
2. 提取后30页代码（约最后9000行）
3. 整理成符合要求的格式

### 场景4: 离线查阅
1. 将txt文件复制到U盘或移动设备
2. 在任何设备上用记事本即可查看
3. 无需安装任何开发环境

## 🛠️ 高级用法

### 使用PowerShell提取特定文件

```powershell
# 提取 app.py 的内容
$content = Get-Content project_source_code.txt
$start = $content | Select-String "文件 1: .\\app.py" | Select-Object -Expand LineNumber
$end = $content | Select-String "文件 2:" | Select-Object -Expand LineNumber
$content[($start+3)..($end-3)] | Out-File extracted_app.py
```

### 使用Python提取特定文件

```python
with open('project_source_code.txt', 'r', encoding='utf-8') as f:
    content = f.read()
    
# 查找 app.py 的内容
import re
pattern = r'文件 1: \.\\app\.py.*?-{60}\s*\n(.*?)\n\n={80}'
match = re.search(pattern, content, re.DOTALL)
if match:
    with open('extracted_app.py', 'w', encoding='utf-8') as out:
        out.write(match.group(1))
```

## 📊 文件统计

- **总文件数**: 112个
- **总行数**: 22,980行
- **总字符数**: 838,762字符
- **文件大小**: 839 KB

### 文件类型分布
- Python文件 (.py): 约40个
- 文档文件 (.md): 约50个
- 脚本文件 (.bat/.ps1): 约15个
- 配置文件: 约7个

## ⚡ 性能优化建议

### 大文件处理
如果文本编辑器打开缓慢：
1. 使用轻量级编辑器（如Notepad++）
2. 关闭语法高亮功能
3. 分段查看（每次查看一个文件）

### 快速定位
1. 使用书签功能标记重要位置
2. 创建索引文件记录各文件起始行号
3. 使用正则表达式进行精确搜索

## 🔄 更新和维护

### 重新生成最新代码
```bash
python export_code_to_txt.py
```

### 验证文件完整性
```bash
# 检查文件大小
dir project_source_code.txt

# 检查行数
powershell -Command "(Get-Content project_source_code.txt | Measure-Object -Line).Lines"
```

## ❓ 常见问题

**Q: 为什么有些文件是空的？**  
A: 某些初始化文件（如 __init__.py）本身就是空的，这是正常的。

**Q: 如何只提取Python文件？**  
A: 可以修改 export_code_to_txt.py 脚本，只包含 .py 扩展名的文件。

**Q: 文件太大打不开怎么办？**  
A: 可以使用命令行工具分割文件，或使用支持大文件的编辑器。

**Q: 编码显示乱码？**  
A: 确保使用UTF-8编码打开文件，大多数现代编辑器都支持。

---

**提示**: 建议将此指南与 project_source_code.txt 放在同一目录下，方便随时查阅。