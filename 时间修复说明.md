# 数据库时间问题修复说明

## 问题描述

之前数据库中的`created_at`字段使用的是UTC时间（协调世界时），而中国使用的是北京时间（UTC+8），导致显示的时间比实际时间少了8小时。

例如：
- 实际创建时间：2026-05-09 22:30（北京时间）
- 数据库显示：2026-05-09 14:30（UTC时间）❌

## 解决方案

### 已实施的修复

1. **修改表结构默认值**
   - 从 `CURRENT_TIMESTAMP`（UTC时间）
   - 改为 `datetime('now', 'localtime')`（本地时间）

2. **修改插入逻辑**
   - 在`add_patient()`函数中显式使用本地时间
   - 确保所有新记录都使用正确的时区

3. **数据迁移**
   - 将现有数据的UTC时间转换为北京时间（+8小时）
   - 自动备份原数据库

### 修复文件

- ✅ [db_manager.py](file:///D:/remindercode/db_manager.py) - 数据库管理模块
- ✅ [fix_time.py](file:///D:/remindercode/fix_time.py) - 时间修复脚本
- ✅ [fix_time.bat](file:///D:/remindercode/fix_time.bat) - 快速修复工具
- ✅ [verify_time.py](file:///D:/remindercode/verify_time.py) - 时间验证工具

## 使用方法

### 方法一：自动修复（推荐）

```bash
# 双击运行或命令行执行
fix_time.bat
```

脚本会自动：
1. 备份现有数据库
2. 重建表结构
3. 转换时间为本地时间
4. 验证修复结果

### 方法二：Python脚本

```bash
python fix_time.py
```

### 验证修复

```bash
python verify_time.py
```

## 技术细节

### SQLite时间函数

```sql
-- UTC时间（旧方式）
DEFAULT CURRENT_TIMESTAMP

-- 本地时间（新方式）
DEFAULT (datetime('now', 'localtime'))
```

### Python时间处理

```python
from datetime import datetime

# 获取本地时间
local_time = datetime.now().strftime('%Y-%m-%d %H:%M:%S')

# UTC转本地时间（+8小时）
from datetime import timedelta
utc_time = datetime.strptime(utc_str, '%Y-%m-%d %H:%M:%S')
local_time = utc_time + timedelta(hours=8)
```

## 修复前后对比

### 修复前 ❌
```
姓名: 张三
创建时间: 2026-05-09 14:30:00  (UTC时间，错误)
系统时间: 2026-05-09 22:30:00  (北京时间)
差异: -8小时
```

### 修复后 ✅
```
姓名: 张三
创建时间: 2026-05-09 22:30:00  (北京时间，正确)
系统时间: 2026-05-09 22:30:00  (北京时间)
差异: 0
```

## 注意事项

1. **备份重要**
   - 修复前会自动备份数据库
   - 备份文件名：`patients_backup_YYYYMMDD_HHMMSS.db`
   - 如需恢复，可从备份文件还原

2. **新数据自动正确**
   - 修复后新增的患者记录会自动使用本地时间
   - 无需额外操作

3. **时区说明**
   - 本系统固定使用北京时间（UTC+8）
   - 如需支持其他时区，需要修改代码

## 常见问题

### Q1: 修复后时间还是不对？

**A:** 检查系统时区设置：
1. 右键任务栏时间 → 调整日期和时间
2. 确认时区为"(UTC+08:00) 北京，重庆，香港特别行政区，乌鲁木齐"
3. 重新运行 `fix_time.bat`

### Q2: 可以恢复到UTC时间吗？

**A:** 可以，从备份文件恢复：
```bash
# 删除当前数据库
del patients.db

# 恢复备份
copy patients_backup_YYYYMMDD_HHMMSS.db patients.db
```

### Q3: 为什么会有8小时差异？

**A:** 
- SQLite的`CURRENT_TIMESTAMP`默认使用UTC时间
- 中国使用北京时间（UTC+8）
- 两者相差8小时

### Q4: 会影响已有功能吗？

**A:** 
- 不会影响任何功能
- 只是时间显示更准确
- 所有数据都已正确迁移

## 预防措施

为避免将来出现类似问题：

1. **统一使用本地时间**
   - 所有时间字段都使用`datetime('now', 'localtime')`
   - 或在Python层统一处理

2. **明确时区标识**
   - 在文档中注明使用的时区
   - 代码注释说明时间处理方式

3. **定期验证**
   - 运行 `verify_time.py` 检查时间准确性
   - 发现偏差及时修复

## 相关文件

- [db_manager.py](file:///D:/remindercode/db_manager.py) - 数据库核心逻辑
- [fix_time.py](file:///D:/remindercode/fix_time.py) - 完整修复脚本
- [fix_time.bat](file:///D:/remindercode/fix_time.bat) - 快速修复工具
- [verify_time.py](file:///D:/remindercode/verify_time.py) - 验证工具

---

**修复完成时间**: 2026-05-09  
**修复版本**: v1.0.1
