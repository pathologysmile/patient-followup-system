# 数据分析看板功能说明

## 功能概述

在智能提醒看板页面新增了完整的数据分析模块，包含关键指标、可视化图表、月度统计和逾期分析四大板块。

## 功能详情

### 1. 📊 关键指标卡片

**显示内容：**
- 👥 **总患者数**：系统中的患者总数
- ✅ **已完成**：已完成回访的患者数量
- ⏳ **待回访**：待回访的患者数量
- 📊 **完成率**：回访完成百分比
- ⚠️ **逾期数**：已过回访日期但未完成的数量

**界面位置：** 数据分析看板顶部

**示例：**
```
┌────────┬────────┬────────┬────────┬────────┐
│ 👥     │ ✅     │ ⏳     │ 📊     │ ⚠️     │
│ 总患者 │ 已完成 │ 待回访 │ 完成率 │ 逾期数 │
│   98   │   53   │   45   │ 54.1%  │   12   │
└────────┴────────┴────────┴────────┴────────┘
```

---

### 2. 🥧 回访完成情况图表

**图表类型：** 柱状图（Streamlit 默认）

**显示内容：**
- 已完成 vs 待回访的数量对比
- 每种状态的详细数量和百分比

**技术实现：**
```python
# 统计各状态数量
status_counts = df_all['status'].value_counts()

# 创建数据框
pie_data = pd.DataFrame({
    '状态': ['已完成', '待回访'],
    '数量': status_counts.values
})

# 显示柱状图
st.bar_chart(pie_data.set_index('状态'))
```

**示例：**
```
🥧 回访完成情况

已完成 ████████████████████ 53 人 (54.1%)
待回访 ██████████████████ 45 人 (45.9%)
```

---

### 3. 📊 病种分布图表

**图表类型：** 柱状图

**显示内容：**
- 前10个最常见疾病的患者数量
- 按患者数降序排列

**技术实现：**
```python
# 统计病种分布（前10个）
diagnosis_counts = df_all['diagnosis'].value_counts().head(10)

diagnosis_df = pd.DataFrame({
    '病种': diagnosis_counts.index,
    '患者数': diagnosis_counts.values
})

st.bar_chart(diagnosis_df.set_index('病种'))
```

**示例：**
```
📊 病种分布

胃癌术后    ████████████████████ 15
糖尿病      ████████████████ 12
高血压      ██████████████ 10
冠心病      ████████████ 8
...
```

---

### 4. 📅 月度回访统计

**显示方式：** 数据表格 + 进度条

**统计维度：**
- 按月分组统计
- 每月总患者数
- 每月完成数
- 每月完成率（带进度条可视化）

**技术实现：**
```python
# 提取月份信息
df_all['created_month'] = pd.to_datetime(df_all['created_at']).dt.to_period('M')

# 按月统计
monthly_stats = df_all.groupby('created_month').agg({
    'id': 'count',
    'status': lambda x: (x == 'completed').sum()
}).rename(columns={'id': '总患者数', 'status': '完成数'})

# 计算完成率
monthly_stats['完成率'] = (monthly_stats['完成数'] / monthly_stats['总患者数'] * 100).round(1)

# 显示表格（带进度条）
st.dataframe(
    monthly_stats.reset_index(),
    column_config={
        "完成率": st.column_config.ProgressColumn(
            "完成率",
            format="%f",
            min_value=0,
            max_value=100,
        )
    }
)
```

**示例：**
```
📅 月度回访统计

┌──────────┬──────────┬────────┬────────────┐
│ 月份     │ 总患者数 │ 完成数 │ 完成率     │
├──────────┼──────────┼────────┼────────────┤
│ 2026-03  │    25    │   20   │ ████ 80.0% │
│ 2026-04  │    30    │   18   │ ███░ 60.0% │
│ 2026-05  │    43    │   15   │ ██░░ 34.9% │
└──────────┴──────────┴────────┴────────────┘
```

---

### 5. ⚠️ 逾期情况分析

**显示内容：**
- 逾期天数分布柱状图
- 平均逾期天数
- 最长逾期天数
- 最短逾期天数

**逾期区间划分：**
- 1-3天
- 4-7天
- 8-15天
- 16-30天
- 30天以上

**技术实现：**
```python
if overdue_patients:
    overdue_df = pd.DataFrame(overdue_patients)
    
    # 按逾期天数分组
    overdue_df['逾期区间'] = pd.cut(
        overdue_df['days_overdue'],
        bins=[0, 3, 7, 15, 30, 999],
        labels=['1-3天', '4-7天', '8-15天', '16-30天', '30天以上']
    )
    
    # 统计分布
    overdue_distribution = overdue_df['逾期区间'].value_counts().sort_index()
    
    # 显示图表
    st.bar_chart(overdue_distribution)
    
    # 显示统计指标
    st.metric("平均逾期天数", f"{overdue_df['days_overdue'].mean():.1f}天")
    st.metric("最长逾期", f"{overdue_df['days_overdue'].max()}天")
    st.metric("最短逾期", f"{overdue_df['days_overdue'].min()}天")
```

**示例：**
```
⚠️ 逾期情况分析

1-3天    ████████████████ 8
4-7天    ██████████ 5
8-15天   ██████ 3
16-30天  ██ 1
30天以上 ░ 0

平均逾期天数: 5.2天
最长逾期: 15天
最短逾期: 1天
```

---

## 使用场景

### 场景1：快速了解整体情况

**需求：** 查看系统的整体运行状况

**操作：**
1. 进入"📊 智能提醒看板"页面
2. 滚动到"📈 数据分析看板"区域
3. 查看顶部的5个关键指标卡片
4. 快速了解总患者数、完成率、逾期数等

### 场景2：分析病种分布

**需求：** 了解哪些疾病患者最多，合理分配资源

**操作：**
1. 查看"📊 病种分布"图表
2. 识别患者数量最多的前几个病种
3. 针对高发病种优化回访策略

### 场景3：追踪月度趋势

**需求：** 观察回访完成率的月度变化

**操作：**
1. 查看"📅 月度回访统计"表格
2. 观察每月的完成率进度条
3. 识别完成率较低的月份，分析原因
4. 制定改进措施

### 场景4：处理逾期问题

**需求：** 分析逾期患者的分布情况

**操作：**
1. 查看"⚠️ 逾期情况分析"区域
2. 观察逾期天数分布图
3. 重点关注长期逾期（16-30天、30天以上）的患者
4. 优先处理短期逾期（1-3天）的患者，防止进一步逾期

---

## 技术亮点

### 1. 实时数据统计

- 所有数据从数据库实时查询
- 自动计算各项指标
- 无需手动更新

### 2. 可视化展示

- 使用 Streamlit 内置图表组件
- 柱状图展示分布情况
- 进度条直观显示完成率

### 3. 多维度分析

- 时间维度：月度统计
- 状态维度：完成/待完成
- 病种维度：疾病分布
- 逾期维度：天数区间

### 4. 用户友好

- 清晰的图标和标签
- 直观的进度条
- 详细的数据说明

---

## 数据流转

```
数据库查询
    ↓
获取所有患者数据
    ↓
Pandas DataFrame 处理
    ↓
┌─────────────────┐
│ 关键指标计算     │ → 总患者数、完成率等
├─────────────────┤
│ 状态统计         │ → 已完成/待回访
├─────────────────┤
│ 病种统计         │ → Top 10 病种
├─────────────────┤
│ 月度分组         │ → 按月统计
├─────────────────┤
│ 逾期分析         │ → 天数区间分布
└─────────────────┘
    ↓
Streamlit 可视化展示
```

---

## 扩展建议

### 未来可添加的功能

1. **每日回访量折线图**
   - 显示每天的回访任务数量
   - 识别高峰期和低谷期

2. **回访完成率趋势图**
   - 显示近30天的完成率变化
   - 预测未来趋势

3. **医生/护士工作量统计**
   - 统计每个医护人员的回访数量
   - 平衡工作负载

4. **患者满意度调查**
   - 收集患者反馈
   - 分析满意度趋势

5. **导出报表功能**
   - 导出Excel/PDF格式的统计报表
   - 支持自定义时间范围

6. **数据对比功能**
   - 本月 vs 上月对比
   - 今年 vs 去年对比

---

## 相关文件

- [app.py](file:///D:/remindercode/app.py#L314-L456) - 数据分析看板实现代码
- [db_manager.py](file:///D:/remindercode/db_manager.py) - 数据查询函数

---

## 注意事项

1. **性能考虑**
   - 当患者数量很大时（>10000），可能需要优化查询
   - 可以考虑缓存统计数据

2. **数据准确性**
   - 确保数据库中的状态字段及时更新
   - 定期检查数据一致性

3. **隐私保护**
   - 统计图表不显示患者个人信息
   - 只展示聚合数据

4. **浏览器兼容性**
   - Streamlit 图表在现代浏览器中表现最佳
   - 建议使用 Chrome、Edge 或 Firefox
