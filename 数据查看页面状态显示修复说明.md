# 数据查看页面状态显示修复说明

## 问题描述

用户反馈：**有的患者已经标记为已完成，但在"患者数据查看"页面中没有显示为完成状态。**

## 问题原因

### 根本原因

系统使用多回访日期结构后，存在**两个状态字段**：

1. **`patients.status`** - 患者表的状态（旧字段）
2. **`visit_plans.status`** - 回访计划表的状态（新字段，实际使用）

### 问题分析

**原逻辑流程：**

1. 在智能看板中标记回访完成 → 更新 `visit_plans.status = 'completed'` ✅
2. 但 `patients.status` 没有同步更新 ❌
3. 数据查看页面读取 `patients.status` → 显示错误的状态 ❌

**示例场景：**

```
患者：张三
回访计划：
  - 2026-05-17: visit_plans.status = 'completed' ✅
  - 2026-06-10: visit_plans.status = 'pending' ⏳
  
patients.status = 'pending' （未更新）

数据查看页面显示：pending ❌（应该根据回访计划计算）
```

## 解决方案

### 1. 修改 `get_all_patients()` 函数（db_manager.py）

**新增逻辑：** 根据患者的所有回访计划动态计算总体状态。

```python
def get_all_patients():
    """获取所有患者数据及其回访计划"""
    # ... 获取患者和回访计划 ...
    
    # 计算患者的整体状态
    has_pending = any(vp[3] == 'pending' for vp in visit_plans)
    all_completed = all(vp[3] == 'completed' for vp in visit_plans) if visit_plans else False
    
    if all_completed and visit_plans:
        overall_status = 'completed'  # 所有计划都完成
    elif has_pending:
        overall_status = 'pending'    # 有计划未完成
    else:
        overall_status = p[8]         # 使用原状态
    
    result.append({
        "status": overall_status,  # ✅ 使用计算后的状态
        "visit_plans": [{
            "plan_id": vp[0],
            "visit_date": vp[1],
            "visit_type": vp[2],
            "status": vp[3],      # ✅ 包含每个计划的详细状态
            "completed_at": vp[4]  # ✅ 包含完成时间
        } for vp in visit_plans]
    })
```

**状态计算规则：**
- ✅ **completed**：所有回访计划都已完成
- ⏳ **pending**：至少有一个回访计划未完成
- 📋 **其他**：使用 patients 表的原始状态

### 2. 增强数据查看页面（app.py）

**新增功能：**

#### A. 回访计划详情区域

显示每个患者的所有回访计划及其状态：

```
📅 回访计划详情

▼ 张三 (男) - 3个回访计划
  - ✅ 2026-05-17 - 常规回访 - 已完成 | 完成时间: 2026-05-17 10:30:00
  - ⏳ 2026-06-10 - 常规回访 - 待回访
  - ⏳ 2026-08-10 - 常规回访 - 待回访

▼ 李四 (女) - 2个回访计划
  - ✅ 2026-05-13 - 常规回访 - 已完成 | 完成时间: 2026-05-13 14:20:00
  - ✅ 2026-05-24 - 常规回访 - 已完成 | 完成时间: 2026-05-24 09:15:00
```

#### B. 操作区增强

选择患者后，显示该患者的所有回访计划，并提供独立操作：

```
🔧 操作区

当前选中：张三 | 性别：男 | 诊断：胃癌术后 | 状态：pending

回访计划列表：
✅ 2026-05-17 - 常规回访 - 已完成
⏳ 2026-06-10 - 常规回访 - 待回访  [✅ 完成]
⏳ 2026-08-10 - 常规回访 - 待回访  [✅ 完成]

[✅ 标记所有为已完成] [🔄 重置所有为待回访]
```

**操作按钮：**
- **单个计划操作**：可以单独标记某个日期的回访为完成/重置
- **批量操作**：标记所有回访为完成/重置所有为待回访

## 界面效果

### 修改前

```
┌─────────────────────────────────┐
│ 📋 患者数据查看                  │
├─────────────────────────────────┤
│ 表格：姓名 | 性别 | 状态 | ...   │
│ 张三   | 男   | pending | ...   │ ← ❌ 错误
│                                 │
│ 🔧 操作区                        │
│ [✅ 标记为已完成]                 │
│ [🔄 重置为待回访]                 │
└─────────────────────────────────┘
```

### 修改后

```
┌─────────────────────────────────┐
│ 📋 患者数据查看                  │
├─────────────────────────────────┤
│ 总患者数: 10 | 待回访: 7 | 已完成: 3 │
├─────────────────────────────────┤
│ 表格：姓名 | 性别 | 状态 | ...   │
│ 张三   | 男   | pending | ...   │ ← ✅ 正确
│ 李四   | 女   | completed| ...  │ ← ✅ 正确
├─────────────────────────────────┤
│ 📅 回访计划详情                  │
│ ▼ 张三 (男) - 3个回访计划        │
│   - ✅ 2026-05-17 - 已完成       │
│   - ⏳ 2026-06-10 - 待回访       │
│   - ⏳ 2026-08-10 - 待回访       │
├─────────────────────────────────┤
│ 🔧 操作区                        │
│ 选择患者: [张三 ▼]               │
│                                 │
│ 回访计划列表：                    │
│ ✅ 2026-05-17 - 已完成           │
│ ⏳ 2026-06-10 - 待回访 [✅ 完成] │
│ ⏳ 2026-08-10 - 待回访 [✅ 完成] │
│                                 │
│ [✅ 标记所有为已完成]             │
│ [🔄 重置所有为待回访]             │
└─────────────────────────────────┘
```

## 技术实现

### 数据库查询优化

**修改前：**
```sql
SELECT visit_date, visit_type, status
FROM visit_plans
WHERE patient_id = ?
```

**修改后：**
```sql
SELECT id, visit_date, visit_type, status, completed_at
FROM visit_plans
WHERE patient_id = ?
ORDER BY visit_date ASC
```

**改进：**
- 增加 `id` 字段：用于唯一标识每个回访计划
- 增加 `completed_at` 字段：显示完成时间
- 按日期排序：方便查看回访进度

### 状态计算逻辑

```python
# 检查是否有未完成的计划
has_pending = any(vp[3] == 'pending' for vp in visit_plans)

# 检查是否所有计划都完成
all_completed = all(vp[3] == 'completed' for vp in visit_plans) if visit_plans else False

# 计算总体状态
if all_completed and visit_plans:
    overall_status = 'completed'
elif has_pending:
    overall_status = 'pending'
else:
    overall_status = p[8]  #  fallback
```

## 优势

### ✅ 解决的问题

1. **状态同步**：数据查看页面现在能正确显示患者的完成状态
2. **详细信息**：可以看到每个回访计划的具体状态和完成时间
3. **灵活操作**：可以单独管理每个回访计划，也可以批量操作

### 🎯 用户体验提升

1. **透明度**：清楚看到哪些日期已完成，哪些待完成
2. **便捷性**：直接在数据查看页面就能标记完成，无需跳转到看板
3. **准确性**：状态实时计算，确保数据一致性

## 相关文件

- [db_manager.py](file:///D:/remindercode/db_manager.py#L109-L170) - 数据查询函数（已优化）
- [app.py](file:///D:/remindercode/app.py#L318-L427) - 数据查看页面（已增强）

## 测试验证

### 测试场景

1. ✅ **单计划完成**：患者只有一个回访计划，标记完成后显示 completed
2. ✅ **多计划部分完成**：患者有多个计划，部分完成时显示 pending
3. ✅ **多计划全部完成**：患者所有计划都完成后显示 completed
4. ✅ **完成时间显示**：已完成的计划显示具体的完成时间
5. ✅ **单独操作**：可以单独标记某个日期的回访为完成
6. ✅ **批量操作**：可以一次性标记所有回访为完成

### 验证步骤

1. 打开系统：[http://localhost:8501](http://localhost:8501)
2. 进入"📋 患者数据查看"页面
3. 检查状态列是否正确显示
4. 展开"📅 回访计划详情"查看详细信息
5. 选择一个患者，测试单独标记完成功能
6. 测试批量标记完成功能

## 注意事项

1. **状态计算是实时的**：每次加载页面都会重新计算状态
2. **不影响数据库结构**：只是改变了状态的读取和显示方式
3. **向后兼容**：旧数据（单回访日期）仍然正常工作
4. **性能优化**：对于大量数据，可以考虑缓存计算结果
